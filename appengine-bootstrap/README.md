This is bootstrap code to generate Google App Engine applications running Go.

To run the application locally you need the install the Go Cloud SDK and
execute the next command from the directory containing this file:

$ goapp serve app.yaml

To deploy the application you have to first create an App Engine project and
use it as the application file in all the yaml files.